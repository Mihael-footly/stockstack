import { LeaderboardClient } from "@/components/site/LeaderboardClient";

export const metadata = { title: "Leaderboard — StockStack" };

export default function LeaderboardPage() {
  return <LeaderboardClient />;
}
