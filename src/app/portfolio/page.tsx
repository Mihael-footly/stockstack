import { PortfolioClient } from "@/components/site/PortfolioClient";

export const metadata = { title: "Portfolio — StockStack" };

export default function PortfolioPage() {
  return <PortfolioClient />;
}
